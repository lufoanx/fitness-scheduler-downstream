name: CI Pipeline

on:
  pull_request:
    branches: [ "main" ]

jobs:
  ci:
    runs-on: ubuntu-latest

    steps:
      - name: Baixar o código
        uses: actions/checkout@v3

      - name: Instalar dependências
        run: |
          npm install
        # Aqui é só ilustrativo para mostrar o processo

      - name: Rodar lint
        run: |
          echo "Rodando lint..."
        # No projeto real teria npm run lint

      - name: Rodar testes
        run: |
          echo "Executando testes..."

      - name: Checar cobertura
        run: |
          echo "Verificando cobertura mínima de 70%..."

      - name: Análise de segurança (SAST)
        run: |
          echo "Executando SAST..."

      - name: Gerar build
        run: |
          echo "Gerando artefato..."
